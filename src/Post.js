import React from 'react';
import PropTypes from 'prop-types';

import HeaderPost from './HeaderPost';

const Post = props => (
  <div className="post">
    <HeaderPost />
  </div>
);

export default Post;
